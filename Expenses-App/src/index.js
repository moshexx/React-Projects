//The first file to run (the root)

// external libraries
import ReactDOM from 'react-dom/client';
// local libraries
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
